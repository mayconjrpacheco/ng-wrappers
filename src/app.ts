import { Component } from '@angular/core';

@Component({
  selector: 'root',
  template: `
    <input-date-wrapper>
      <input type="text">
    </input-date-wrapper>
  `
})
export class AppComponent {

}
